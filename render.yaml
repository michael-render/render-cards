databases:
  - name: render-cards-db
    plan: basic-256mb
    databaseName: render_cards

services:
  - type: workflow
    runtime: node
    name: rendervous-cards-workflow
    plan: starter
    buildCommand: npm ci
    startCommand: node workflows/tasks.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false

  - type: web
    runtime: node
    name: rendervous-cards
    plan: starter
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /api/health
    disk:
      name: card-images
      mountPath: /var/data/cards
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: render-cards-db
          property: connectionString
      - key: CARD_STORAGE_PATH
        value: /var/data/cards
      # Render Workflows — create a workflow service manually in Dashboard
      - key: RENDER_API_KEY
        sync: false
      - key: RENDER_OWNER_ID
        sync: false
      - key: RENDER_REGION
        value: oregon
