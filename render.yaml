databases:
  - name: render-cards-db
    plan: basic-256mb
    databaseName: render_cards

services:
  - type: web
    runtime: node
    name: render-stat-cards
    plan: starter
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /api/health
    disk:
      name: card-images
      mountPath: /var/data/cards
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: RENDER_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: render-cards-db
          property: connectionString
      - key: CARD_STORAGE_PATH
        value: /var/data/cards
      # Workflow service (render-cards-workflow) must be created manually
      # in the Dashboard — Blueprints don't support type: workflow
